{
    "$schema": "http://json-schema.org/schema",
    "$id": "SchematicsAppWorksAuth",
    "title": "AppWorks Authentication Schema",
    "type": "object",
    "properties": {
      "otds_url": {
        "type": "string",
        "description": "Please enter your OTDS authentication rest service URL. (ex: http://<< host/ip >>:<< port >>/otdsws/rest/authentication/credentials)",
        "x-prompt": "Please enter your OTDS authentication rest service URL."
      },
      "project": {
        "type": "string",
        "description": "The name of the project.",
        "$default": {
          "$source": "projectName"
        }
      },
      "path": {
        "type": "string",
        "format": "path",
        "description": "The path to create the service.",
        "visible": false
      }
    },
    "required": ["otds_url"]
  }